// place files you want to import through the `$lib` alias in this folder.
// Re-export components that should be available to the entire app
export { SpeedInsights } from '@vercel/speed-insights/svelte';
